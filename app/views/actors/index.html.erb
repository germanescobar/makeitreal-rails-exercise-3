<% @actors.each do |actor| %>
  <article>
    <img src="<%= actor.image_url %>" alt="Actor photo">
    <h2><%= actor.name %></h2>
    <p><%= actor.bio %></p>
    <span><%= actor.birth_date %></span>
    <span><%= actor.birth_place %></span>
  </article>
<% end %>
